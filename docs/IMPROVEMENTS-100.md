# 100 improvements: making OnCo exhaustively useful

Written 2026-09-09 against the corpus at `npm run validate` = 3,948 entities in 18 kinds (64 cancers, 377 products, 92 targets, 348 technologies, 401 trials, 295 companies, 415 people, 1,107 ideas, 146 key papers) and roughly 8,000 static pages. Everything below is checked against `src/data/hub-ideas.ts` (all three waves), `docs/GAPS.md`, `src/lib/nav.ts`, `CHANGELOG.md` and the code: nothing here is already built or marked `shipped`. Where an item realises a `planned` or `proposed` hub idea, the row says so.

## How to read and execute this

- **Side data over schema changes.** Every new dataset is a keyed side file (`src/data/<thing>.ts`, the pattern of `payloads.ts`, `biomarkers.ts`, `regional-approvals.ts`, `trial-outcomes.ts`) validated against the graph at build time. No item requires a change to `src/lib/schema.ts`; promotion into the schema can follow later.
- **Shared files.** `src/lib/schema.ts`, `src/data/index.ts`, `src/components/EntityDetail.tsx`, `src/app/[kind]/page.tsx`, `src/lib/nav.ts`, `src/app/layout.tsx` and `package.json` are edited only by the main session after merge. Rows that need them say **wiring:** in the files column; the agent ships the component or data plus a standalone route so the work is testable before wiring. Every new route also needs a `nav.ts` entry, which is left implicit.
- **Batches.** Ten batches of ten. Each batch owns a disjoint set of files (listed under each heading) so ten agents can run in ten worktrees without touching each other's files. Batch letters are used in the `Batch` column.
- **Effort.** S = under a day for one agent, M = one to three days, L = a week or a sustained data effort.

## The ten batches

| Batch | Title | Files it owns (new unless noted) |
|---|---|---|
| A | Patients and families | `src/app/{prep,side-effects,symptoms,second-opinion,survivorship,assistance,report-reader}/`, `src/data/{side-effect-guidance,symptom-paths,referral-routes,survivorship,assistance-schemes,report-fields,red-flags}.ts`, `src/lib/{side-effects,prep,trial-explain,eligibility}.ts`, `src/components/{PrepPack,SymptomPath,TrialExplainer,SecondOpinion,SurvivorshipPlan,ReportReader,RedFlagCard}.tsx`; existing `src/components/{CaregiverPanel,TrialFinderGeo}.tsx`, `src/lib/ctgov.ts` |
| B | Clinician power tools | `src/app/{regimens,biomarker-matrix,sequencing,guidelines,calculators,staging,interactions,irae}/`, `src/data/{regimens,guideline-versions,guideline-map,staging,interactions,irae}.ts`, `src/lib/{regimens,sequencing,calculators,interactions}.ts`, `src/components/{DoseScheduleDiagram,BiomarkerMatrix,GuidelineConcordance,RiskScore}.tsx` |
| C | Scientist tools | `src/app/{dossiers,pathway-drugs,models,open-questions,preprints,preclinical-models,assays}/`, `src/app/resistance/gaps/`, `src/data/{target-xrefs,model-registry,open-questions,hotspots,preclinical-models,assays}.ts`, `src/components/{Dossier,XrefStrip,HotspotPlot}.tsx`, `scripts/fetch-preprints.ts`, `.github/workflows/refresh-preprints.yml` |
| D | Builders and investors | `src/app/{catalysts,scorecards,deals,exclusivity,manufacturing,pipeline,market,sponsors,build}/`, `src/data/{catalysts,deals,exclusivity,manufacturing,company-financials,setting-shares,sponsor-aliases}.ts`, `src/lib/{ics,company-score,pipeline-stats,market}.ts`, `src/components/DealFlow.tsx`, `public/openapi.yaml` |
| E | Data quality and currency checks | `scripts/{freshness,check-links,numbers,history,apply-factcheck}.ts`; existing `scripts/{audit,factcheck}.ts`; `src/app/{freshness,history,schema}/`; existing `src/app/audit/`, `src/components/ProvenanceLine.tsx`; `src/lib/{corpus-rules.test,schema-docs}.ts`; `.github/{CODEOWNERS,labeler.yml,PULL_REQUEST_TEMPLATE.md}`, `.github/workflows/{ci,links,label}.yml` |
| F | Site mechanics | existing `src/components/{EntityBrowser,PivotTable,CommandPalette,Tabs,PrintButton,SuggestEdit}.tsx`, `src/app/globals.css`, `scripts/build-api.ts`, `src/app/api/page.tsx`, `src/data/i18n/*.ts`; new `src/app/{saved,sitemap.ts,robots.ts}`, `src/lib/{saved-views,csv,qr}.ts`, `src/components/{WatchButton,DownloadTable,RegisterSW}.tsx`, `public/{manifest.webmanifest,sw.js}`, `scripts/{build-feeds,a11y,i18n-coverage}.ts`, `.zenodo.json`, `.github/workflows/{release,a11y}.yml` |
| G | Visuals | `src/data/{organ-schematics,journeys,km-curves,spread}.ts`; existing `src/data/{animated,pathways}.ts`; `src/components/{OrganSchematic,JourneyGantt,KMChart,ForestPlot,SpreadMap,TrendChart,ChartExport}.tsx`; existing `src/components/{Pictogram,ProcessSchematic,PathwayDiagram,CasesByCountry}.tsx`; `src/app/{atlas,journeys,forest,molecules}/`; existing `src/app/{body,cases}/`, `scripts/fetch-globocan.ts` |
| H | Community | `src/app/{review,reviewers,idea-votes,teach,contributors,newsletter}/`; existing `src/app/gaps/`, `src/data/reviews.ts`, `src/components/{ReviewBadge,TldrText}.tsx`, `scripts/provenance.ts`; new `src/data/{adoptions,i18n/reviewed}.ts`, `src/lib/review-queue.ts`, `src/components/{DiscussLink,SlideDeck}.tsx`, `scripts/{gen-issue-forms,new-record,fetch-votes,newsletter}.ts`, `.github/ISSUE_TEMPLATE/*.yml`, `.github/DISCUSSION_TEMPLATE/` |
| I | AI, search and graph power | `src/app/{search,ask,path}/`, `src/lib/{semantic,ask,gql,paths}.ts` (+ tests), `scripts/{embed,build-similar,build-triples,build-context}.ts`, `src/data/wikidata-ids.ts`, `public/llms.txt`, `mcp/**`; existing `src/components/{SearchBox,QueryBuilder,Neighbours,PowerView}.tsx`, `src/lib/{relevance,biomarker-match,term-hover}.ts(x)` |
| J | Intelligence feeds and currency automation | `scripts/{fetch-fda,fetch-ema,fetch-abstracts,fetch-hta,fetch-pulse,fetch-citations,fetch-survival,propose-updates}.ts`; existing `scripts/fetch-trials.ts`, `src/data/regional-approvals.ts`, `src/components/RegulatoryBrowser.tsx`; `src/app/{hta,survival,status}/`; existing `src/app/{regulatory,calendar,digests,pulse,papers}/`; `.github/workflows/{refresh-fda,refresh-regional,refresh-hta,refresh-pulse,propose}.yml` |

## The 100 improvements

| # | Title | Why it matters | What to build | Audience | Effort | Files touched | Batch |
|---|---|---|---|---|---|---|---|
| 1 | Appointment prep pack builder | Patients arrive with scattered notes; a chosen, printable question list is the single most requested artefact (hub-ideas2 #13, still `planned`). | New `/prep/`: pick a cancer with `CancerPicker`, tick questions from `questionsFor()` in `src/lib/questions.ts`, add your own, pull current treatments from the browser profile (`src/lib/profile.ts`), print or download one page. State lives in `localStorage` under `onco:prep:v1`. | Patients, carers | M | src/app/prep/page.tsx, src/components/PrepPack.tsx, src/lib/prep.ts | A |
| 2 | Side-effect lookup, symptom first | 377 products carry grade 3+ rates, but a person with a symptom does not search by drug. | `src/lib/side-effects.ts` indexes every `drug.toxicity[].event` into normalised symptom groups; `/side-effects/` lets a reader pick a symptom (or their profile drugs) and see which treatments cause it, how often, and the threshold to act, from `src/data/side-effect-guidance.ts` (CTCAE-derived plain language: "call today if...", sourced). | Patients, carers, nurses | M | src/app/side-effects/page.tsx, src/lib/side-effects.ts, src/data/side-effect-guidance.ts | A |
| 3 | Symptom-to-test pathways | People with a worrying symptom want to know which test comes next and what it rules out. | `src/data/symptom-paths.ts`: about 40 presenting symptoms, each mapping to possible cancers (cancer ids), first-line tests (technology ids such as colonoscopy, low-dose CT, PSA, mammography) and referral criteria with NICE NG12 and USPSTF sources. `/symptoms/` renders a stepper; ids are validated at build like `body-regions.ts`. | Patients, GPs | M | src/app/symptoms/page.tsx, src/data/symptom-paths.ts, src/components/SymptomPath.tsx | A |
| 4 | Plain-language trial explainer | Hazard ratios mean nothing to most readers; absolute differences and NNT do (hub-ideas #12, still `planned`). | `src/lib/trial-explain.ts` turns each `trial.outcomes[]` row into sentences ("62 vs 51 out of 100 alive at 3 years; 11 more per 100; one extra person helped for every 9 treated"), flags surrogate endpoints (pCR, PFS) versus OS, and states population caveats from `setting`. `TrialExplainer.tsx` renders above the pictogram; vitest covers %, months and HR-only rows. | Patients, journalists | M | src/components/TrialExplainer.tsx, src/lib/trial-explain.ts, src/lib/trial-explain.test.ts; wiring: EntityDetail trial tab | A |
| 5 | Second-opinion finder | Expert centres exist per cancer, but readers need "who, where, and how do I get referred in my country". | `/second-opinion/`: choose cancer and country (reader's region from `src/lib/region.tsx`); combine `ExpertCentres` logic, `src/lib/trial-leadership.ts` and people with matching `specialisms`, plus `src/data/referral-routes.ts` describing how second opinions work in the US, UK NHS, EU, AU, JP and CN, with remote-review services, costs and sources. | Patients, carers | M | src/app/second-opinion/page.tsx, src/data/referral-routes.ts, src/components/SecondOpinion.tsx | A |
| 6 | Survivorship and late-effects planner | Survivorship pages exist as technologies, but there is no per-treatment surveillance plan. | `src/data/survivorship.ts`: per treatment class or drug id (anthracyclines, chest radiotherapy, checkpoint inhibitors, alkylators, cisplatin, CAR-T) list late effects, the screening test, the interval and the source (COG LTFU, ASCO, ESMO). `/survivorship/` builds a printable plan from the profile's prior lines or manual picks. | Survivors, GPs | M | src/app/survivorship/page.tsx, src/data/survivorship.ts, src/components/SurvivorshipPlan.tsx | A |
| 7 | Financial help and assistance navigator | `drug.access[].assistance` is scattered across 377 pages; financial toxicity is a listed bottleneck. | `/assistance/` aggregates every `access[]` row by country and drug (manufacturer programmes, generics, reimbursement) with the 14 `patient-org` collections and national schemes from `src/data/assistance-schemes.ts` (NHS, Medicare Part D cap, PBS and others, sourced). Filter by country and drug; print a one-page list. | Patients, social workers | M | src/app/assistance/page.tsx, src/data/assistance-schemes.ts | A |
| 8 | Pathology report reader | Reports full of Ki-67, margins, IHC scores and LVI cannot be decoded by the people they are about. | `src/data/report-fields.ts` maps about 60 report fields to glossary term ids, value ranges and "what it changes"; `/report-reader/` is a per-cancer form (breast, prostate, colorectal, lung, lymphoma, myeloma) where the reader enters values and gets a plain-language reading with links; nothing is stored. | Patients | M | src/app/report-reader/page.tsx, src/data/report-fields.ts, src/components/ReportReader.tsx | A |
| 9 | Red-flag "when to call" cards per drug | Caregiver mode lists toxicities but not the specific emergencies (fever on chemotherapy, CRS, ILD cough, grade 3 diarrhoea). | `src/data/red-flags.ts` keyed by drug id or modality (ICI, DXd ADCs, CAR-T, bispecifics, cytotoxics): symptom, threshold, action, source (boxed warnings, ASCO). `RedFlagCard.tsx` is a printable wallet card rendered inside `CaregiverPanel.tsx`. | Carers, patients | S | src/data/red-flags.ts, src/components/RedFlagCard.tsx, src/components/CaregiverPanel.tsx | A |
| 10 | Trial eligibility pre-screener | The live finder shows trials but not whether a person could plausibly join one. | Extend `src/lib/ctgov.ts` to request `eligibilityModule` and `armsInterventionsModule`; `src/lib/eligibility.ts` parses common criteria (ECOG, prior lines, brain metastases, measurable disease, biomarker) into a checklist scored likely / unclear / unlikely against the profile; `TrialFinderGeo.tsx` shows it and always links the full criteria. | Patients, trial coordinators | M | src/lib/ctgov.ts, src/lib/eligibility.ts (+ test), src/components/TrialFinderGeo.tsx | A |
| 11 | Regimen library with doses and schedules | Clinicians think in regimens (FOLFIRINOX, R-CHOP, KEYNOTE-522), which exist only as prose inside standard-of-care rows. | `src/data/regimens.ts`: about 80 regimens with components (drug ids), dose and unit, days, cycle length, cycles, route, setting, cancer ids, emetogenicity, G-CSF need, source (label, protocol or trial). `/regimens/` is an `EntityBrowser`-style table with facets by cancer, setting and intent; every id is validated at build. | Clinicians, pharmacists | L | src/app/regimens/page.tsx, src/data/regimens.ts, src/lib/regimens.ts | B |
| 12 | Dose-schedule diagrams | A cycle drawn as a calendar strip is understood faster than "D1, D8 q21d". | `DoseScheduleDiagram.tsx`: SVG strip of one cycle (days by component) with repeat count and total duration, used on `/regimens/` and `/regimens/<id>/`. Reduced-motion safe and prints in black and white. | Clinicians, patients | S | src/components/DoseScheduleDiagram.tsx, src/app/regimens/[id]/page.tsx | B |
| 13 | Biomarker-to-therapy matrix | The tumour board answers one profile; clinicians also want the whole grid. | `/biomarker-matrix/`: rows are the 39 biomarkers in `src/data/biomarkers.ts`, columns are cancers; each cell lists approved products (region-aware via `regional-approvals.ts`) versus trial-only, computed from `matchRows()` in `src/lib/biomarker-match.ts`. Click-through opens the tumour board with the cell pre-selected. | Oncologists, pathologists | M | src/app/biomarker-matrix/page.tsx, src/components/BiomarkerMatrix.tsx | B |
| 14 | Line-of-therapy sequencing tables | Navigator personalises one path; clinicians want the full line 1 to line 3 map per subgroup. | `/sequencing/`: per cancer, a table of lines by biomarker subgroup built from `standardOfCare` rows (reusing the setting parser in `Navigator.tsx`) and `pairing` records of type `sequence` and `caution`, with guideline chips and evidence scores. | Oncologists | M | src/app/sequencing/page.tsx, src/lib/sequencing.ts (+ test) | B |
| 15 | Guideline version diffs | Guideline chips give today's category; what changed between versions is what changes practice. | `src/data/guideline-versions.ts`: per cancer, dated NCCN and ESMO version entries listing the changed rows (added, removed, recategorised) sourced to the update summaries. `/guidelines/<cancer>/` shows a two-version diff view. | Clinicians, educators | M | src/app/guidelines/page.tsx, src/app/guidelines/[id]/page.tsx, src/data/guideline-versions.ts | B |
| 16 | NCCN / ESMO / NICE / ASCO concordance | GAPS notes there is no slot for NICE, ASCO, CSCO or JSMO; disagreements between bodies are clinically important. | `src/data/guideline-map.ts` keyed `cancerId#setting` with entries per body (recommendation, grade, date, URL). `/guidelines/` gains a concordance table highlighting where bodies differ. Side data now; promotion into `standardOfCare.guideline` later. | Clinicians, payers | M | src/data/guideline-map.ts, src/app/guidelines/page.tsx, src/components/GuidelineConcordance.tsx | B |
| 17 | Clinical calculators | BSA, Calvert, creatinine clearance and RECIST arithmetic is done on phones on ad-laden sites. | `/calculators/`: pure client tools in `src/lib/calculators.ts` (Mosteller and DuBois BSA, Calvert carboplatin AUC, Cockcroft-Gault, ANC, corrected calcium, RECIST 1.1 percentage change, cumulative anthracycline dose, dose banding) with unit tests and formula citations; each result has a copy button. | Clinicians, pharmacists | M | src/app/calculators/page.tsx, src/lib/calculators.ts, src/lib/calculators.test.ts | B |
| 18 | Staging and risk scores per cancer | Stage groupings and IPI, R-ISS, IMDC and FLIPI scores are referenced everywhere and stated nowhere. | `src/data/staging.ts`: AJCC 8, Ann Arbor, Lugano and myeloma staging groupings plus standard risk scores per cancer with inputs, points and outcome ranges, sourced. `/staging/` renders tables and interactive scorers and links each stage to the matching standard-of-care setting. | Clinicians, students | L | src/app/staging/page.tsx, src/data/staging.ts, src/components/RiskScore.tsx | B |
| 19 | Drug interaction and contraindication lookup | TKIs with CYP3A4 inhibitors, QT stacking and PPI-TKI absorption are frequent, avoidable errors. | `src/data/interactions.ts`: per drug id, interacting classes or agents, mechanism, severity, management and source (label section 7). `/interactions/` checker: pick two or more products, see flagged pairs, plus food, QT and hepatic flags. | Clinicians, pharmacists | M | src/app/interactions/page.tsx, src/data/interactions.ts, src/lib/interactions.ts | B |
| 20 | Immune-related adverse event guide | Checkpoint inhibitors sit in most standard-of-care rows; irAE management by organ and grade is the most looked-up guidance. | `src/data/irae.ts`: organ system by grade, giving hold or continue, steroid dose, escalation, rechallenge, sourced to ASCO, NCCN and ESMO. `/irae/` renders an organ selector with a printable card and links to the ICI product pages and `/toxicity/`. | Clinicians, nurses | M | src/app/irae/page.tsx, src/data/irae.ts | B |
| 21 | Target dossiers | Target pages show biology and products; scientists need everything about a target in one long-form dossier. | `/dossiers/<targetId>/` generated from the graph: biology, prevalence table, products by modality and phase, trials, resistance mechanisms from `src/data/resistance.ts`, pathways whose nodes carry `targetId`, key papers, companies, open questions and external ids. Static params from the 92 targets. | Scientists, BD | M | src/app/dossiers/[id]/page.tsx, src/components/Dossier.tsx | C |
| 22 | External identifier cross-refs for targets | Without UniProt, Ensembl, ChEMBL and Open Targets ids the graph cannot be joined to the rest of biology. | `src/data/target-xrefs.ts`: per target id, HGNC, Ensembl, UniProt, ChEMBL target, Open Targets, CIViC, OncoKB and COSMIC links. Rendered as an "Elsewhere" strip on dossiers; exported alongside the entity JSON by merging in `Dossier.tsx`. | Scientists, builders | S | src/data/target-xrefs.ts, src/components/XrefStrip.tsx | C |
| 23 | Pathway-to-drug matrix | Pathway diagrams light up per product; the inverse question, which nodes have no drug, needs a table. | `/pathway-drugs/`: for each of the 44 pathways, nodes by products hitting the node's `targetId` (via `src/lib/pathway-products.ts`), phase-coloured, with undrugged nodes highlighted and counts per pathway. | Scientists, investors | S | src/app/pathway-drugs/page.tsx | C |
| 24 | Model and dataset registry | Foundation models and datasets exist as technologies and collections without comparable structured fields. | `src/data/model-registry.ts` keyed by existing ids: parameters, modality (histology, single-cell, DNA, multimodal), training data, weights availability, licence, benchmark results, paper DOI; datasets get size, access tier and consent notes. `/models/` is a faceted table. | Scientists, builders | M | src/app/models/page.tsx, src/data/model-registry.ts | C |
| 25 | Open questions per target and technology | Open problems exist on cancers only; targets and technologies have none. | `src/data/open-questions.ts`: per target or technology id, questions with why they are unresolved, what evidence would answer them, linked trials and ideas, and a named source. Rendered on dossiers and indexed at `/open-questions/` with filters by stage and actor. | Scientists, funders | M | src/data/open-questions.ts, src/app/open-questions/page.tsx | C |
| 26 | Preprint tracker | Europe PMC indexes bioRxiv and medRxiv; preprints are where targets move first. | `scripts/fetch-preprints.ts` uses `restUrl(..., { preprints: true })` from `src/lib/europepmc.ts` for targets, drugs and technologies and writes `public/preprints/<id>.json`. `/preprints/` lists the last 90 days under a "not peer-reviewed" banner and shows which later appeared as journal articles. Weekly workflow. | Scientists | M | scripts/fetch-preprints.ts, src/app/preprints/page.tsx, .github/workflows/refresh-preprints.yml | C |
| 27 | Mutation hotspot lollipop plots | KRAS G12C versus G12D and EGFR ex19del, L858R, T790M and C797S decide which drug works; nothing shows it. | `src/data/hotspots.ts`: per target, protein length, domains, hotspot residues with frequency (COSMIC or cBioPortal source) and the drugs addressing each; `HotspotPlot.tsx` is an SVG lollipop with drug chips per residue, shown on dossiers. | Scientists, clinicians | M | src/data/hotspots.ts, src/components/HotspotPlot.tsx | C |
| 28 | Preclinical model availability | Which cell lines, PDX and GEMMs exist per cancer and target is a daily lab question. | `src/data/preclinical-models.ts`: per cancer or target, common lines with DepMap ids, PDX repositories (PDXNet, EurOPDX, Champions), GEMMs and organoid banks, with links. `/preclinical-models/` table with facets. | Scientists | M | src/app/preclinical-models/page.tsx, src/data/preclinical-models.ts | C |
| 29 | Companion diagnostic and assay registry | Cutoffs (PD-L1 CPS 10, HER2 IHC 1+ or 2+, MSI, TMB 10) gate every biomarker-selected drug. | `src/data/assays.ts`: FDA and CE-marked companion diagnostics and key assays: name, vendor, platform, analyte, cutoff, linked drug ids and cancers, approval date, source (FDA CDx list). `/assays/` table linking back to the biomarker matrix. | Pathologists, scientists | M | src/app/assays/page.tsx, src/data/assays.ts | C |
| 30 | Unaddressed resistance routes | Resistance mechanisms without countermeasures are the drug-design opportunities. | `/resistance/gaps/`: computed from `src/data/resistance.ts`: every mechanism with zero `countermeasures` or only preclinical refs, grouped by class and linked to ideas that target it; adds a gaps count to each class card. | Scientists, investors | S | src/app/resistance/gaps/page.tsx | C |
| 31 | Catalyst calendar per company | The readout calendar holds 15 events; investors track catalysts by company with dates and an ICS feed. | `src/data/catalysts.ts`: dated catalysts (PDUFA, readout, advisory committee, label decision, patent ruling) with company id, drug id, confidence and source. `/catalysts/` filters by company and quarter, exports `.ics` via `src/lib/ics.ts`, and merges the existing `calendar.ts` events for display. | Investors, BD | M | src/app/catalysts/page.tsx, src/data/catalysts.ts, src/lib/ics.ts | D |
| 32 | Company scorecards | Company pages list products; a disclosed, comparable score is missing. | `src/lib/company-score.ts`: approved products, phase 3 assets, distinct targets and modalities, regions approved, failures, regulatory events in 24 months, trial counts from `public/trials/index.json`. `/scorecards/` ranks the 295 companies with the formula shown, like the institution ranking. | Investors, BD | M | src/app/scorecards/page.tsx, src/lib/company-score.ts, src/lib/company-score.test.ts | D |
| 33 | Deal and licensing map | China-out ADC deals reshaped the field; deals appear only in prose in `companies.ts`. | `src/data/deals.ts`: date, licensor and licensee (company ids), asset (drug id), type (licence, acquisition, option, co-development), upfront, total, territories, source. `/deals/` table plus a flow diagram by year and region (`DealFlow.tsx`, SVG chord). | Investors, BD, journalists | M | src/app/deals/page.tsx, src/data/deals.ts, src/components/DealFlow.tsx | D |
| 34 | Patent and exclusivity expiry | Loss of exclusivity drives generics, biosimilars and prices. | `src/data/exclusivity.ts`: per drug, key patent and regulatory exclusivity expiry by region (Orange and Purple Book, EMA), biosimilar or generic entrants with dates, source. `/exclusivity/` timeline chart 2026 to 2040 with filters by modality and company. | Investors, payers | M | src/app/exclusivity/page.tsx, src/data/exclusivity.ts | D |
| 35 | Manufacturing capacity map | Isotopes have a supply tracker; ADC conjugation, viral vector and cell therapy sites do not. | `src/data/manufacturing.ts`: sites (CDMOs and in-house) with modality capability, location, capacity notes, customers (company ids) and source. `/manufacturing/` renders on `WorldMap.tsx` with a modality filter and links to the relevant bottleneck. | Investors, builders, policy | M | src/app/manufacturing/page.tsx, src/data/manufacturing.ts | D |
| 36 | Pipeline funnel and crowding index | How many assets chase TROP2 or CLDN18.2 by phase is the core competitive question. | `/pipeline/`: funnel by phase for a chosen target, modality or cancer from product statuses plus `public/trials/index.json`; a crowding index per target (assets divided by addressable patients) with the formula disclosed in `src/lib/pipeline-stats.ts`. | Investors, BD | M | src/app/pipeline/page.tsx, src/lib/pipeline-stats.ts | D |
| 37 | Addressable population estimator | Market sizing multiplies incidence by biomarker prevalence by line; all three already exist separately. | `src/lib/market.ts`: GLOBOCAN incidence (`src/lib/globocan.ts`) times `target.prevalence` times setting share (editorial, sourced in `src/data/setting-shares.ts`) per region gives patients per year. `/market/` shows the calculation with every input linked and a wide uncertainty band. | Investors, planners | M | src/app/market/page.tsx, src/lib/market.ts, src/data/setting-shares.ts | D |
| 38 | Company financial snapshots | Tickers exist; oncology revenue, R&D spend and cash are what analysts want. | `src/data/company-financials.ts`: per public company, fiscal year, oncology revenue, top product sales, R&D spend, cash, source (10-K, 20-F or annual report). Shown on `/scorecards/` and exported. | Investors | M | src/data/company-financials.ts, src/app/scorecards/page.tsx | D |
| 39 | Trial sponsor leaderboard | Who runs the most phase 3 trials per cancer is a signal missing from institution and company pages. | `/sponsors/`: aggregate `sponsor` across corpus trials and `public/trials/*.json` studies, normalised to company or institution ids via `src/data/sponsor-aliases.ts`; counts by phase, status and cancer. | Investors, cooperative groups | S | src/app/sponsors/page.tsx, src/data/sponsor-aliases.ts | D |
| 40 | Builder cookbook and OpenAPI spec | The API page lists files; builders need recipes, types and a spec. | `/build/`: recipes (trial matcher, dashboard, chatbot over MCP), `public/openapi.yaml` describing `/api/v1/`, a copyable TypeScript type import snippet, embed widget gallery, licence and attribution notes. | Builders | S | src/app/build/page.tsx, public/openapi.yaml | D |
| 41 | Freshness SLAs per kind | `/audit/` lists staleness but nothing defines "too old" per kind or fails CI. | `scripts/freshness.ts`: thresholds (approved products 90 days, recruiting trials 120, calendar 30, cancers 180), writes `public/freshness.json`; `/freshness/` shows counts by review track; a CI step fails when critical stale exceeds a limit. | Maintainers | S | scripts/freshness.ts, src/app/freshness/page.tsx, .github/workflows/ci.yml | E |
| 42 | Citation link validator with archiving | Thousands of `links[].url` rot silently. | `scripts/check-links.ts`: HEAD or GET every URL (rate-limited, retried), record status, redirects and domain changes; request Wayback saves and store archive URLs in `public/links.json`; weekly workflow opens a PR; `/audit/` gains a broken-links section. | Maintainers, readers | M | scripts/check-links.ts, .github/workflows/links.yml, src/app/audit/page.tsx | E |
| 43 | Contradiction detector v2 | Current audit checks are name and status level; cross-field contradictions slip through. | Extend `scripts/audit.ts`: `approvals` versus `regional-approvals.ts`; status `approved` without approval rows; trial `positive` with HR at or above 1 on the primary endpoint; `asOf` older than the latest `regulatoryEvents.date`; spike duplicates whose scalars diverge (hidden by `mergeDuplicates`); roadmap step refs pointing at withdrawn products. | Maintainers | M | scripts/audit.ts, scripts/audit.test.ts | E |
| 44 | Unsourced-number detector | Rule one is "cite it"; nothing checks that numbers in text have a source. | `scripts/numbers.ts` extracts percentages, hazard ratios, months and dollar figures from `tldr`, `summary` and `result`; flags records with numbers but no `links`, and outcomes without `source`; writes medium-severity findings into `public/audit.json`. Groundwork for source-per-sentence (hub-ideas2 #31). | Maintainers, reviewers | S | scripts/numbers.ts, scripts/audit.ts | E |
| 45 | Reviewer workflow automation | Review tracks are documented in `.github/REVIEWERS.md` but not enforced. | `.github/CODEOWNERS` maps `src/data/spikes/*`, `drugs.ts` and `targets.ts` to track teams; `labeler.yml` labels PRs by kind touched; the PR template gains a checklist (source, asOf, CORRECTIONS line); an Action comments the affected page URLs derived from the diff. | Maintainers, reviewers | S | .github/CODEOWNERS, .github/labeler.yml, .github/workflows/label.yml, .github/PULL_REQUEST_TEMPLATE.md | E |
| 46 | Per-record change history | Provenance shows the last edit; readers want what changed on this record over time. | `scripts/history.ts`: for records touched in the last 200 commits, run `git log -p` on their file and diff the record's fields into `public/history/<id>.json`; `/history/` is a recent-changes feed with field-level diffs; `ProvenanceLine.tsx` links to it. | Readers, reviewers | M | scripts/history.ts, src/app/history/page.tsx, src/components/ProvenanceLine.tsx | E |
| 47 | Registry sync and patch proposals | Fact checks report mismatches; nobody applies them. | Extend `scripts/factcheck.ts` to compare trial status, phase and primary completion against ClinicalTrials.gov and emit `public/factcheck-patches.json` (id, field, current, proposed, source); `scripts/apply-factcheck.ts` applies accepted patches to the data file and appends a `CORRECTIONS.md` line. | Maintainers | M | scripts/factcheck.ts, scripts/apply-factcheck.ts | E |
| 48 | Corpus rule tests | Only four invariants are tested in `graph.test.ts`; editorial rules live in prose. | `src/lib/corpus-rules.test.ts`: approved products have at least one approval with region and year; phase 3 trials have `nct`; standard-of-care rows have refs; `asOf` is not in the future; guideline URLs point at guideline domains; TL;DRs contain no acronym from a denylist unless it is a glossary term. | Maintainers | S | src/lib/corpus-rules.test.ts | E |
| 49 | Orphan and near-duplicate detector | Parallel agents create near-duplicate records and unlinked orphans. | Add to `scripts/audit.ts`: entities with no incoming and at most one outgoing link; name or `aka` collisions across kinds; names within Levenshtein distance 2 inside a kind; products sharing a `code`. Surfaced at `/audit/` with merge suggestions. | Maintainers | S | scripts/audit.ts | E |
| 50 | Data dictionary page | The schema is readable only as TypeScript. | `/schema/`: at build, walk every Zod schema with `z.toJSONSchema` (Zod 4) via `src/lib/schema-docs.ts` and render kind, field, type and description with an example record per kind; links to the JSON Schema file emitted by item 56. | Builders, contributors | S | src/app/schema/page.tsx, src/lib/schema-docs.ts | E |
| 51 | Saved views | Table state is lost on reload; hub-ideas2 #21 is still `proposed`. | `EntityBrowser.tsx` serialises facets, search and sort into `?v=` (base64 JSON) with `history.replaceState`, as `PowerView.tsx` already does; "Save view" stores name and URL in `localStorage` via `src/lib/saved-views.ts`; `/saved/` lists saved views and the watchlist. | Everyone | M | src/components/EntityBrowser.tsx, src/lib/saved-views.ts, src/app/saved/page.tsx | F |
| 52 | Watchlist with "changed since you looked" | Notifications are `proposed`; a static site can still show diffs since the last visit. | `WatchButton.tsx` (star, `localStorage`) records the entity id and last-seen `editedOn`; `/saved/` compares against `public/provenance.json` and `public/history/` to show what changed. Mounted in the `SuggestEdit.tsx` toolbar, which already renders on every object page. | Patients, investors | M | src/components/WatchButton.tsx, src/components/SuggestEdit.tsx, src/app/saved/page.tsx | F |
| 53 | CSV and JSON export on every table | Analysts copy tables by hand. | `src/lib/csv.ts` plus `DownloadTable.tsx` (CSV and JSON of the currently filtered rows, filename with date, licence line as a comment); wired into `EntityBrowser.tsx` and `PivotTable.tsx`; other tables adopt the component later. | Analysts, builders | S | src/lib/csv.ts, src/components/DownloadTable.tsx, src/components/EntityBrowser.tsx, src/components/PivotTable.tsx | F |
| 54 | Keyboard navigation | Power users want row navigation and go-to chords beyond the ⌘K palette. | `CommandPalette.tsx` gains a shortcut layer: `/` focuses search, `j` and `k` move row focus in `EntityBrowser` (rows get `data-row`), `Enter` opens, `g c`, `g d`, `g t` go to cancers, products and trials, `?` opens a shortcuts sheet; ignores typing in inputs and respects screen readers. | Clinicians, analysts | S | src/components/CommandPalette.tsx, src/components/EntityBrowser.tsx | F |
| 55 | Offline PWA | hub-ideas2 #30 is `proposed`; a static site is the easy case. | `public/manifest.webmanifest`, `public/sw.js` (cache the shell, `/api/v1/search.json`, visited pages, structures on demand), `RegisterSW.tsx` client component and an offline banner. The manifest link is one line in the `layout.tsx` metadata. | Patients in clinics, low-bandwidth users | M | public/manifest.webmanifest, public/sw.js, src/components/RegisterSW.tsx; wiring: layout.tsx | F |
| 56 | Bulk export and DOI release | hub-ideas2 #28 (`proposed`): CSV, Parquet, Zenodo. | `scripts/build-api.ts` also emits `/api/v1/<kind>.csv`, `all.ndjson` and `schema.json` (from `z.toJSONSchema`); `.zenodo.json` and a `release.yml` workflow upload a tagged corpus to Zenodo for a DOI; `/api/` documents the new files. | Scientists, builders | M | scripts/build-api.ts, .zenodo.json, .github/workflows/release-dataset.yml, src/app/api/page.tsx | F |
| 57 | Sitemap, robots and Atom feeds | About 8,000 pages with no sitemap, robots file or feed. | `src/app/sitemap.ts` and `src/app/robots.ts` (compatible with `output: "export"`); `scripts/build-feeds.ts` writes Atom feeds for the changelog, regulatory events, calendar and pulse to `public/feeds/*.xml`; feed links on the About page. | Search engines, subscribers | S | src/app/sitemap.ts, src/app/robots.ts, scripts/build-feeds.ts | F |
| 58 | Accessibility audit in CI | Theme, contrast and skip link shipped; nothing checks regressions. | `scripts/a11y.ts` runs axe-core through Playwright on 30 representative built pages in CI; fixes found so far: `aria-sort` and `scope` in `EntityBrowser.tsx`, focus trap and `aria-activedescendant` in `CommandPalette.tsx`, `role="tablist"` arrow keys in `Tabs.tsx`, contrast tokens in `globals.css`. | All readers | M | scripts/a11y.ts, .github/workflows/a11y.yml, src/components/EntityBrowser.tsx, src/components/CommandPalette.tsx, src/components/Tabs.tsx, src/app/globals.css | F |
| 59 | Print packs | Print shows the active tab only; patients need every section, a date and a QR code back to the page. | `Tabs.tsx` renders all panels with `print:block`; `PrintButton.tsx` offers "this section, full page, patient pack"; a print header carries asOf, a URL QR code from `src/lib/qr.ts` (inline SVG) and the disclaimer; table page-break rules go into the `@media print` block of `globals.css`. | Patients, clinicians | S | src/components/Tabs.tsx, src/components/PrintButton.tsx, src/app/globals.css, src/lib/qr.ts | F |
| 60 | i18n coverage and new languages | Translations cover about 450 of 3,948 ids in four languages. | `scripts/i18n-coverage.ts` reports coverage per kind and language; extend ES, ZH, PT and HI to all cancers, products, targets and technologies; add FR, DE, JA and AR (RTL rules in `globals.css`); the machine-assisted flag stays until reviewed (item 78). | Non-English readers | L | src/data/i18n/*.ts, scripts/i18n-coverage.ts, src/app/globals.css; wiring: [kind]/[id]/page.tsx for hreflang | F |
| 61 | Organ-level schematics per cancer | The body map is whole-body; each cancer needs its organ, subsites and regional node stations. | `src/data/organ-schematics.ts`: per cancer, SVG paths for the organ, subsites and regional nodes with labels linked to `subtypes`; `OrganSchematic.tsx`; gallery at `/atlas/organs/`; mounted in the cancer Overview after wiring. | Patients, students | L | src/data/organ-schematics.ts, src/components/OrganSchematic.tsx, src/app/atlas/organs/page.tsx; wiring: EntityDetail cancerTabs | G |
| 62 | Treatment journey diagrams | "What do the next twelve months look like" is the first patient question. | `src/data/journeys.ts`: per cancer and stage, phases (diagnosis, neoadjuvant, surgery, adjuvant, surveillance) with typical durations, drug and technology ids and decision points; `JourneyGantt.tsx` SVG; `/journeys/<cancer>/`. | Patients, carers | M | src/data/journeys.ts, src/components/JourneyGantt.tsx, src/app/journeys/[id]/page.tsx | G |
| 63 | Survival curves from published KM data | Pictograms show one timepoint; curves show the shape and durability of benefit. | `src/data/km-curves.ts`: per trial arm, published landmark survival points (6, 12, 24, 36, 60 months) with source; `KMChart.tsx` draws step curves with median markers; `TrialOutcomes` in `Pictogram.tsx` renders it when data exist. | Clinicians, patients | M | src/data/km-curves.ts, src/components/KMChart.tsx, src/components/Pictogram.tsx | G |
| 64 | Forest plots per cancer and setting | 401 trials carry HRs and CIs; nobody can see them side by side. | `/forest/`: pick cancer, setting and endpoint; `ForestPlot.tsx` draws HR with CI per trial from `outcomes[]`, sized by N, coloured by evidence score, linking to trial pages; CSV of the plotted rows. | Clinicians, scientists | M | src/app/forest/page.tsx, src/components/ForestPlot.tsx | G |
| 65 | Process schematics for missing modalities | Animated schematics cover ADCs, CAR-T and radioligands; bispecifics, degraders, oncolytic viruses, mRNA vaccines, TIL and TCR-T have none. | Extend `src/data/animated.ts` with step lists and meshes for the six modalities; `ProcessSchematic.tsx` selects by `modality` and `technologies`; a test asserts every `modality` string in `drugs.ts` maps to a schematic. | Patients, students | M | src/data/animated.ts, src/components/ProcessSchematic.tsx, src/data/animated.test.ts | G |
| 66 | Metastatic spread maps | Where a cancer spreads decides surveillance imaging and the symptoms to watch. | `src/data/spread.ts`: per cancer, common metastatic sites with frequency ranges and sources; `SpreadMap.tsx` overlays arrows on the `BodyMap` silhouette; shown in `/body/` region panels and at `/atlas/spread/`. | Patients, students | M | src/data/spread.ts, src/components/SpreadMap.tsx, src/app/body/page.tsx, src/app/atlas/spread/page.tsx | G |
| 67 | Incidence and mortality trend charts | Cases by country is a snapshot; trends tell the story. | `TrendChart.tsx` sparklines and line charts from GLOBOCAN time series (extend `scripts/fetch-globocan.ts` to keep multiple editions under `public/globocan/`); shown on `/cases/` and inside country rows in `CasesByCountry.tsx`. | Policy, journalists | M | src/components/TrendChart.tsx, src/app/cases/page.tsx, src/components/CasesByCountry.tsx, scripts/fetch-globocan.ts | G |
| 68 | Molecule gallery | Rotating molecules are the site's signature, yet there is no place to browse them all. | `/molecules/`: grid of every `public/structures/*` entry via `MoleculeThumb.tsx` with filters by modality, payload class, target and approval status; click opens the viewer; a "structure missing" list drives the next `fetch-structures.ts` run. | Everyone | S | src/app/molecules/page.tsx | G |
| 69 | Six new pathway diagrams | 44 pathways exist; DNA damage response, Hedgehog, Notch, Hippo, cGAS-STING and TGF-beta are referenced but undrawn. | Add six `pathway` records to `src/data/pathways.ts` with nodes (`targetId` where a target exists), edges, analogy, interventions and links; the existing `PathwayDiagram` renders them and lights up per product. | Scientists, students | M | src/data/pathways.ts | G |
| 70 | Chart export as SVG and PNG | Every chart is an SVG that readers screenshot. | `ChartExport.tsx` wraps any chart with "Download SVG / PNG" (serialise the SVG, rasterise via canvas, append a licence and source footer); adopted by `Pictogram.tsx`, `KMChart`, `ForestPlot`, `PathwayDiagram.tsx` and `TrendChart`. | Educators, journalists | S | src/components/ChartExport.tsx, src/components/Pictogram.tsx, src/components/PathwayDiagram.tsx | G |
| 71 | Review queue and coverage dashboard | `src/data/reviews.ts` has no entries; nobody can see which pages most need review. | `/review/`: coverage per kind and track; a queue ranked by graph degree, evidence tier and staleness (`src/lib/review-queue.ts`); "Review this page" opens a prefilled `review.yml` issue with the checklist from `.github/REVIEWERS.md`. | Reviewers, maintainers | M | src/app/review/page.tsx, .github/ISSUE_TEMPLATE/review.yml, src/lib/review-queue.ts | H |
| 72 | Reviewer roster and badge levels | Badges name a reviewer; a roster page and links to person records make them credible. | `reviews.ts` gains an optional `personId`; `/reviewers/` lists reviewers with pages reviewed, tracks and COI; `ReviewBadge.tsx` links to the roster and shows a level by count of reviews. | Readers, reviewers | S | src/data/reviews.ts, src/app/reviewers/page.tsx, src/components/ReviewBadge.tsx | H |
| 73 | Issue templates for every change type | Four templates exist; stale facts, readouts, regional approvals and translation fixes have none. | Add `stale-fact.yml`, `trial-readout.yml`, `regional-approval.yml`, `translation-fix.yml` and `accessibility.yml` with structured fields (entity id, source URL, date); `scripts/gen-issue-forms.ts` keeps the kind dropdowns in sync with `KINDS`. | Contributors | S | .github/ISSUE_TEMPLATE/*.yml, scripts/gen-issue-forms.ts | H |
| 74 | Record scaffolder and good-first records | Copying a neighbouring record is error-prone for newcomers. | `npm run new -- --kind drug --name "X"` (`scripts/new-record.ts`) writes a validated stub with required fields and a TODO checklist; `/gaps/` gains a "good first records" list from audit findings with size labels. | Contributors | S | scripts/new-record.ts, src/app/gaps/page.tsx; wiring: package.json script | H |
| 75 | Discussion threads per object | Suggested edits become issues; open discussion has no home. | A GitHub Discussions category "Objects"; `DiscussLink.tsx` builds a search-or-create URL by entity id; discussion template in `.github/DISCUSSION_TEMPLATE/`; placed beside Suggest an edit after wiring. | Community | S | src/components/DiscussLink.tsx, .github/DISCUSSION_TEMPLATE/objects.yml; wiring: EntityDetail | H |
| 76 | Idea voting and adoption tracking | hub-ideas3 #18 is `proposed`; 1,107 ideas need a signal of interest and of uptake. | `scripts/fetch-votes.ts` reads GitHub Discussion reactions per idea id into `public/votes.json`; `/idea-votes/` ranks ideas by votes and shows `adopted` records from `src/data/adoptions.ts` (who picked it up, date, source). | Community, funders | M | scripts/fetch-votes.ts, src/app/idea-votes/page.tsx, src/data/adoptions.ts | H |
| 77 | Contributors page | Provenance credits editors per record; nothing thanks them in one place. | `/contributors/`: from `git log` via `scripts/provenance.ts` output plus `reviews.ts`: commits, records touched, kinds, first and last contribution, all-contributors style keys; links to person records where they exist. | Community | S | src/app/contributors/page.tsx, scripts/provenance.ts | H |
| 78 | Translation review track | Every translation is unreviewed machine output. | `src/data/i18n/reviewed.ts` (id, language, reviewer, date); `TldrText.tsx` shows "reviewed by" or "machine-assisted"; a `translation-review.yml` issue form; coverage on `/review/`. | Non-English readers, translators | S | src/data/i18n/reviewed.ts, src/components/TldrText.tsx, .github/ISSUE_TEMPLATE/translation-review.yml | H |
| 79 | Teaching packs | hub-ideas2 #49 is `proposed`; educators rebuild slides from pages by hand. | `/teach/<id>/` for cancers and fronts: an HTML slide deck (print to PDF) generated from tldr, standard of care, history, pipeline and schematics, plus a quiz drawn from `src/data/benchmark.ts` questions whose entity ids match; speaker notes cite sources. | Educators, students | M | src/app/teach/page.tsx, src/app/teach/[id]/page.tsx, src/components/SlideDeck.tsx | H |
| 80 | Weekly issue generator and signup | The changelog is the newsletter, but nothing is rendered or sent. | `scripts/newsletter.ts` renders the week's changelog, regulatory events, calendar and pulse to `public/newsletter/<date>.html` and Atom; `/newsletter/` archive with a Buttondown or Listmonk signup form and no tracking. | Subscribers | S | scripts/newsletter.ts, src/app/newsletter/page.tsx | H |
| 81 | Semantic search | MiniSearch misses paraphrases ("drug for HER2-low breast cancer"). | `scripts/embed.ts` embeds name plus tldr with a small local model (transformers.js MiniLM) at build into `public/api/v1/embeddings.bin` (about 4k by 384 floats); `src/lib/semantic.ts` does cosine search in the browser; `/search/` combines lexical and semantic results with an explanation per hit; `SearchBox.tsx` falls back to it when lexical returns fewer than three. | Everyone | M | scripts/embed.ts, src/lib/semantic.ts, src/app/search/page.tsx, src/components/SearchBox.tsx; wiring: package.json dependency | I |
| 82 | Ask OnCo, grounded and cited | Readers ask questions; answers must cite pages and never invent. | `/ask/`: retrieve top-k entities (lexical plus semantic), assemble an extractive answer from tldr, summary and outcomes with a citation per sentence; optional bring-your-own API key (kept in the browser) for synthesis constrained to the retrieved text; scored against `src/data/benchmark.ts` with `scripts/benchmark-run.ts`. | Patients, clinicians | L | src/app/ask/page.tsx, src/lib/ask.ts, src/lib/ask.test.ts | I |
| 83 | MCP server v2 | Five tools cover lookup; agents need the power tools too. | Add `biomarker_match`, `trials_near`, `compare`, `regimens`, `calendar`, `toxicity_compare` and `path_between`; resources per kind; prompts ("tumour board prep", "investor brief"); publish as `onco-mcp` from its own `mcp/package.json`; README with Claude and Cursor configuration. | Builders, AI users | M | mcp/server.ts, mcp/tools/*.ts, mcp/package.json, mcp/README.md | I |
| 84 | Text graph query language | The form-based query builder caps expressiveness. | `src/lib/gql.ts`: parser for `cancer:tnbc -> drugs(status:approved) -> targets` with filters, hops and projections, fully tested; `QueryBuilder.tsx` gains a text mode with autocomplete and a shareable URL; results reuse the existing table. | Analysts, scientists | M | src/lib/gql.ts, src/lib/gql.test.ts, src/components/QueryBuilder.tsx | I |
| 85 | "Pages like this" | Neighbours are explicit links; similarity finds the non-obvious. | `scripts/build-similar.ts`: Jaccard on neighbour sets plus tag overlap (embeddings later) into `public/api/v1/similar.json` (top 8 per id); `Neighbours.tsx` shows a "Similar" strip listing the shared links that explain each match. | Everyone | S | scripts/build-similar.ts, src/components/Neighbours.tsx | I |
| 86 | Path finder between any two objects | "How is Hippo related to sacituzumab?" is a graph question the site cannot answer. | `/path/?from=&to=`: BFS over `graph()` with edge labels from `Backlink.via`; renders up to three shortest paths as chips with each relationship named; linked from Compare. | Scientists, students | S | src/app/path/page.tsx, src/lib/paths.ts, src/lib/paths.test.ts | I |
| 87 | Profile-aware ranking | Explore ranks by cancer only; the profile already knows stage, biomarkers and country. | `src/lib/relevance.ts` gains optional signals (biomarker match via `biomarker-match.ts`, approved in the reader's region, stage-matched standard-of-care row); `PowerView.tsx` gets a "Personalise" toggle that shows the added points; nothing leaves the browser. | Patients, clinicians | M | src/lib/relevance.ts, src/lib/biomarker-match.ts, src/components/PowerView.tsx | I |
| 88 | Auto-link entities in prose | Term hovers cover glossary words; drug, target and cancer names in summaries stay plain text. | Extend `src/lib/term-hover.tsx` with an Aho-Corasick matcher over all entity names and `aka` (longest match, once per paragraph, skip self) linking to the page with the kind's hover card; tests for overlaps such as "HER2" versus "HER2-low". | Everyone | M | src/lib/term-hover.tsx, src/lib/term-hover.test.ts | I |
| 89 | RDF triples with Wikidata ids | hub-ideas #5 (Wikidata cross-linking) is `proposed`; a triple export is the first half. | `src/data/wikidata-ids.ts` (QIDs for cancers, products, targets, companies, institutions); `scripts/build-triples.ts` emits `public/api/v1/onco.nt` using schema.org terms and `owl:sameAs`; documented on `/api/` by batch F. | Builders, Wikimedians | M | src/data/wikidata-ids.ts, scripts/build-triples.ts | I |
| 90 | LLM-ready context files | AI crawlers and RAG builders want clean text per page. | `scripts/build-context.ts` writes `public/api/v1/context/<id>.md` (tldr, summary, structured fields, sources, neighbours) and `public/llms.txt` following the llms.txt convention. | Builders, AI users | S | scripts/build-context.ts, public/llms.txt | I |
| 91 | FDA approvals feed | New approvals reach the corpus only when someone notices (daraxonrasib sat stale for two weeks). | `scripts/fetch-fda.ts`: openFDA `drugsfda` plus the FDA OCE approvals page into `public/fda/recent.json`; `/regulatory/` shows "new since last build" and "not yet in corpus" candidates with a one-click issue link; weekly workflow. | Everyone | M | scripts/fetch-fda.ts, .github/workflows/refresh-fda.yml, src/app/regulatory/page.tsx, src/components/RegulatoryBrowser.tsx | J |
| 92 | Trial status change detection | Recruiting to completed, and posted results, are the earliest readout signals. | Extend `scripts/fetch-trials.ts` to diff against the previous snapshot (status, `hasResults`, primary completion date) into `public/trials/changes.json`; `/calendar/` lists auto-detected changes as "expected" events pending editorial review. | Investors, clinicians | M | scripts/fetch-trials.ts, src/app/calendar/page.tsx | J |
| 93 | EMA, MHRA and PMDA feeds for regional approvals | Regional approvals are hand-verified; EMA and MHRA publish machine-readable tables. | `scripts/fetch-ema.ts` (EMA medicines CSV, MHRA products API, PMDA new approvals list) matches by INN to product ids, writes gaps as proposals to `public/regional/candidates.json`, and stamps verified rows in `regional-approvals.ts` with the fetch date. | Clinicians, investors | M | scripts/fetch-ema.ts, src/data/regional-approvals.ts, .github/workflows/refresh-regional.yml | J |
| 94 | Congress abstract harvest | Digests are written by hand after each congress. | `scripts/fetch-abstracts.ts`: Europe PMC meeting-abstract records (JCO and Annals supplements) matched to product and trial ids into `public/digests/candidates.json`; `/digests/` shows candidates for the next congress with "promote to digest" links. | Editors, clinicians | M | scripts/fetch-abstracts.ts, src/app/digests/page.tsx | J |
| 95 | HTA decisions feed | Cost and access rows lack payer verdicts (NICE, CDA, G-BA, HAS, PBAC). | `scripts/fetch-hta.ts` (NICE API, PBAC outcomes, G-BA decisions where public) into `public/hta/*.json`; `/hta/` table of verdicts per product and country with dates and links, cross-linked to the assistance navigator. | Payers, patients, investors | M | scripts/fetch-hta.ts, src/app/hta/page.tsx, .github/workflows/refresh-hta.yml | J |
| 96 | Automated research pulse | `pulse.ts` is hand-curated and dated `pulseAsOf`. | `scripts/fetch-pulse.ts` pulls RSS or Atom from FDA, NEJM, Lancet Oncology, JCO, Nature Medicine, Endpoints and STAT, matches titles to entity ids by name and `aka`, and writes `public/pulse/auto.json`; `/pulse/` shows the automated stream beside the curated themes. | Everyone | M | scripts/fetch-pulse.ts, src/app/pulse/page.tsx, .github/workflows/refresh-pulse.yml | J |
| 97 | Citation counts for key papers | 146 key papers have DOIs but no impact signal. | `scripts/fetch-citations.ts` (OpenAlex works by DOI) into `public/openalex/papers.json` with total and yearly citations; `/papers/` gains a "most cited key papers" board. | Scientists | S | scripts/fetch-citations.ts, src/app/papers/page.tsx | J |
| 98 | Survival and mortality statistics | Incidence is shown; survival by cancer, stage and country is the number patients ask for. | `scripts/fetch-survival.ts` (SEER 5-year relative survival by stage, CONCORD-3, EUROCARE) into `public/survival/*.json`; `/survival/` tables with stage, period and caveats, linked from cancers. | Patients, policy | M | scripts/fetch-survival.ts, src/app/survival/page.tsx | J |
| 99 | Nightly change-proposal bot | Feeds create candidates; a bot should draft the edits for humans to review. | `scripts/propose-updates.ts` reads factcheck patches, trial changes, FDA and regional candidates, drafts record patches with source URLs (LLM optional and constrained to fetched text), opens a PR labelled `bot-proposal` with a `CORRECTIONS.md` line, and never merges. | Maintainers | L | scripts/propose-updates.ts, .github/workflows/propose.yml | J |
| 100 | Data currency status page | Nobody can see when each feed last ran or failed. | `/status/`: build time, each feed's `fetched` timestamp and counts (trials, papers, openalex, globocan, fda, hta, pulse, preprints), stale-record counts, CI badge and an incident log; `src/lib/feed-meta.ts` reads the `public/*.json` metadata. | Maintainers, builders | S | src/app/status/page.tsx, src/lib/feed-meta.ts | J |

## Twenty highest-value items

Ranked by reach multiplied by how often the need recurs, weighted toward things no other public site does well.

1. **#11 Regimen library with doses and schedules** (with #12 dose diagrams): the missing clinical backbone; every standard-of-care row becomes executable.
2. **#2 Side-effect lookup, symptom first**: turns 377 toxicity tables into the question patients actually have.
3. **#4 Plain-language trial explainer**: absolute differences and NNT on 401 trials; closes hub-ideas #12.
4. **#1 Appointment prep pack builder**: the printable artefact patients ask for; closes hub-ideas2 #13.
5. **#91 FDA approvals feed**: the fastest-moving fact type, currently updated by hand.
6. **#99 Nightly change-proposal bot**: makes every feed (#91 to #96) actionable without adding editorial load.
7. **#41 Freshness SLAs per kind**: defines "current" and fails the build when it is not.
8. **#13 Biomarker-to-therapy matrix**: the whole precision-oncology grid on one screen.
9. **#63 Survival curves from published KM data** and **#64 forest plots**: the two visuals clinicians trust most, from data already structured.
10. **#21 Target dossiers** (with #22 cross-refs, #25 open questions, #27 hotspots): the scientist's landing page.
11. **#31 Catalyst calendar per company**: the investor's daily tool, with ICS export.
12. **#33 Deal and licensing map**: the story of the last three years in one dataset.
13. **#36 Pipeline funnel and crowding index**: the competitive question every builder and investor asks.
14. **#82 Ask OnCo, grounded and cited**: natural-language entry to the whole corpus with a citation per sentence.
15. **#53 CSV and JSON export on every table**: cheapest possible win for analysts and builders.
16. **#57 Sitemap, robots and Atom feeds**: 8,000 pages are under-indexed today.
17. **#42 Citation link validator with archiving**: the trust layer depends on links that do not rot.
18. **#71 Review queue and coverage dashboard**: `reviews.ts` is empty; this is how it fills.
19. **#17 Clinical calculators**: small, daily, and drives clinician return visits.
20. **#51 Saved views** with **#52 watchlist**: the first step from reference site to daily tool.
