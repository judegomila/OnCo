import type { CompanyInput } from "@/lib/schema";

/** Venture-backed companies attacking cancer, beyond Y Combinator. Filled from the research pass; see the file header once populated. */
export const companiesStartups: CompanyInput[] = [];
