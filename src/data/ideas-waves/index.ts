import type { IdeaInput } from "@/lib/schema";

/** Waves of ideas attacking the bottlenecks of the war on cancer. Register each wave file here. */
const files: IdeaInput[][] = [];
export const ideaWaves: IdeaInput[] = files.flat();
